# List of fruits
fruits = ["apple", "banana", "cherry", "blueberry", "orange"]

# Extract the first letter of each fruit and join them into a single string
first_letters_string = '*'.join([fruit[0] for fruit in fruits])

# Print the resulting string
print(first_letters_string)
